<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/new_photo_fragment"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="15dp"
    android:gravity="center"
    android:orientation="vertical"
    tools:context=".NewPhotoFragment">


    <ImageView
        android:id="@+id/selected_image"
        android:layout_width="wrap_content"
        android:layout_height="150dp"
        android:layout_alignParentStart="true"
        android:contentDescription="@string/selected_image"
        app:srcCompat="@drawable/select_or_upload_picture" />

    <ImageButton
        android:id="@+id/take_photo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        android:contentDescription="@string/image_capture"
        android:src="@drawable/take_photo" />

    <!--TODO: Decide if we need the texts
    <TextView-->
    <!--android:id="@+id/take_photo_text"-->
    <!--android:layout_width="wrap_content"-->
    <!--android:layout_height="wrap_content"-->
    <!--android:layout_below="@id/take_photo"-->
    <!--android:gravity="center"-->
    <!--android:text="@string/use_camera" />-->

    <ImageButton
        android:id="@+id/upload_photo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/take_photo"
        android:layout_centerHorizontal="true"
        android:layout_margin="15dp"
        android:contentDescription="@string/insert_photo"
        android:src="@drawable/choose_photo_gallery" />

    <!--<TextView -->
    <!--android:layout_width="wrap_content"-->
    <!--android:layout_height="wrap_content"-->
    <!--android:layout_below="@id/upload_photo"-->
    <!--android:layout_centerInParent="true"-->
    <!--android:text="@string/use_gallery" />-->

    <Button
        android:id="@+id/confirm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/upload_photo"
        android:layout_centerInParent="true"
        android:layout_margin="15dp"
        android:gravity="center_horizontal"
        android:text="@string/confirm" />

</RelativeLayout>
